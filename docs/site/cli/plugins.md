# plugins

List installed plugins.

## Usage

```bash
doing plugins [OPTIONS]
```

## Options

| Flag | Description |
| --- | --- |
| `--type TYPE` | Filter by plugin type (export, import) |

## Examples

List all installed plugins:

```bash
doing plugins
```

List only export plugins:

```bash
doing plugins --type export
```
